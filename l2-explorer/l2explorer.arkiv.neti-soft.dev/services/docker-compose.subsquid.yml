services:
  squid-db:
    image: postgres:17
    restart: always
    command: postgres -c 'max_connections=200' -c 'client_connection_check_interval=60000'
    volumes:
      - squid-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U blockscout -d blockscout"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  l1-processor:
    image: golemnetwork/subsquid-bridging:main
    restart: always
    command: sqd process:prod -- "Ethereum Hoodi"
    depends_on: [squid-db]
    env_file:
      - ../envs/subsquid-bridging.env

  l2-processor:
    image: golemnetwork/subsquid-bridging:main
    restart: always
    command: sqd process:prod -- "Arkiv L2"
    depends_on: [squid-db]
    env_file:
      - ../envs/subsquid-bridging.env

  l3-processor:
    image: golemnetwork/subsquid-bridging:main
    restart: always
    command: sqd process:prod -- "Arkiv Kaolin L3"
    depends_on: [squid-db]
    env_file:
      - ../envs/subsquid-bridging.env

  graphql-engine:
    image: golemnetwork/subsquid-bridging:main
    restart: always
    command: sqd serve:prod
    depends_on: [squid-db]
    env_file:
      - ../envs/subsquid-bridging.env
    environment:
      - VIRTUAL_HOST=squid.l2explorer.arkiv.neti-soft.dev
      - VIRTUAL_PORT=4350
      - LETSENCRYPT_HOST=squid.l2explorer.arkiv.neti-soft.dev
      - LETSENCRYPT_EMAIL=kacper.zuk@neti-soft.com

volumes:
  squid-db-data:
